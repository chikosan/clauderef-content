---
title: "Claude Code v2.1.259: safer unattended runs and managed MCP"
date: 2026-09-03
tags: [claude-code, release, security, mcp, automation]
summary: "Claude Code adds fail-closed permission prompts for headless automation, organization-managed remote MCP servers, machine-readable plugin validation, and fixes for concurrent sessions and permission boundaries."
---

Claude Code **v2.1.259**, published September 2, 2026, is a practical operations release. The standout changes are safer non-interactive execution, centralized MCP provisioning, and several fixes that matter when many sessions or automated jobs share the same environment.

## Changes worth noticing

- **Fail-closed headless automation:** `--permission-prompts none` automatically denies anything that would require a prompt while leaving the active permission mode—including auto mode—to decide requests it can handle without interaction. This is useful for unattended CI, runners, and other headless hosts where a hidden prompt would otherwise stall work.
- **Organization-managed MCP:** the new `managedMcpServers` managed setting lets administrators provide HTTP or SSE MCP servers to every user using the same entry shape as `.mcp.json`. Entries that try to launch a local command are skipped, keeping this managed path focused on remote servers.
- **Machine-readable plugin checks:** `claude plugin validate --json` adds structured validation output, making plugin validation easier to integrate into CI gates and release automation.
- **Safer concurrent sessions:** parallel Claude Code sessions no longer silently overwrite one another's `~/.claude.json` updates, reducing the risk of losing workspace trust, MCP, or project state when several sessions run at once.
- **Permission and cache fixes:** Bash `Read()` deny rules now cover more option values, file operands, compound commands, and recursive directory reads. OAuth refresh no longer invalidates the prompt cache in telemetry-disabled sessions.

## Operational details

The release also fixes custom command and skill `model:` frontmatter being ignored in interactive sessions, makes auto mode keep the session model when a requested frontmatter model is unsupported, improves resume behavior for malformed attachment entries, and tightens startup behavior around managed settings. Remote and scheduled workflows also receive permission and connector reliability fixes.

## What to do

Update Claude Code, then test unattended jobs with an explicit permission policy before changing production runners:

```bash
claude update
claude -p "run the repository checks" --permission-prompts none
```

For enterprise MCP rollout, verify the managed-server configuration on a small group first and keep local-command MCP servers in the existing approved configuration path. For plugin pipelines, prefer the new JSON validation output once the exact flag is present in your installed v2.1.259 CLI.

The v2.1.259 changelog currently documents these new surfaces ahead of the dedicated CLI and settings reference pages, so treat their exact interface as version-specific until those reference pages catch up.

[Official Claude Code v2.1.259 release notes](https://github.com/anthropics/claude-code/releases/tag/v2.1.259) · [Official Claude Code changelog](https://code.claude.com/docs/en/changelog) · [CLI reference](https://code.claude.com/docs/en/cli-reference) · [Settings](https://code.claude.com/docs/en/settings)
